#include "main.h"

/**
 * _isdigit - prints a digit
 * @c: variable
 * Return: 1 if c, otherwise 0
 */

int _isdigit(int c)
{
	if (_isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
